meta {
  name: API Root
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response contains API info", function() {
    expect(res.getBody()).to.have.property('message');
    expect(res.getBody().message).to.include('MCP SEM-4AI Reporter REST API');
  });
}